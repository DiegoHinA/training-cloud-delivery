[tox]
envlist = py39
skipsdist = True
 
[testenv]
deps =
    pytest
    pytest-cov
    -r{toxinidir}/requirements.txt
commands = pytest --cov=app --cov-report=app/coverage.xml --cov-config=tox.ini --cov-branch tests.py
 
[coverage:run]
